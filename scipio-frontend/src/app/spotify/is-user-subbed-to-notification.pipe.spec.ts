import { IsUserSubbedToNotificationPipe } from './is-user-subbed-to-notification.pipe';

describe('IsUserSubbedToNotificationPipe', () => {
  it('create an instance', () => {
    const pipe = new IsUserSubbedToNotificationPipe();
    expect(pipe).toBeTruthy();
  });
});
