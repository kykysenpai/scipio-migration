import { CalculateCreditChangePipe } from './calculate-credit-change.pipe';

describe('CalculateCreditChangePipe', () => {
  it('create an instance', () => {
    const pipe = new CalculateCreditChangePipe();
    expect(pipe).toBeTruthy();
  });
});
