import { AmountLeftPipePipe } from './amount-left-pipe.pipe';

describe('AmountLeftPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AmountLeftPipePipe();
    expect(pipe).toBeTruthy();
  });
});
