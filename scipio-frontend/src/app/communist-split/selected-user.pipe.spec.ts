import { SelectedUserPipe } from './selected-user.pipe';

describe('SelectedUserPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectedUserPipe();
    expect(pipe).toBeTruthy();
  });
});
